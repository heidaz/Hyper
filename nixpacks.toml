[phases.setup]
aptPkgs = ["nodejs", "npm"]

[phases.install]
cmds = ["npm install"]
dependsOn = ["setup"]

[phases.build]
cmds = ["npm run build"]
dependsOn = ["install"]

[start]
cmd = "node server.js" 
